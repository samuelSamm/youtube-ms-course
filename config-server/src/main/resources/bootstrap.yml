server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/samuelSamm/ms-course-youtube.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
      #  bootstrap: true
      enabled: true
  security:
    user:
      name: root
      password: s3cr3t
