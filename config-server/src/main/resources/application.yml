server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/samuelSamm/youtube-ms-course.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
      #bootstrap: true
      #enabled: true
  security:
    user:
      name: root
      password: s3cr3t
