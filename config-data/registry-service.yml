server:
  port: 8099

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defultZone: http://${eureka.instance.hostname}:${server.port}/eureka/